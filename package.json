{
  "name": "aruizca-resume",
  "version": "1.0.0",
  "type": "module",
  "description": "AI-powered resume and cover letter generator",
  "private": true,
  "packageManager": "pnpm@10.14.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "setup": "pnpm install && pnpm install --filter=\"./packages/*\"",
    "build": "turbo run build",
    "build:check": "turbo run build:check",
    "dev": "concurrently \"pnpm watch\" \"pnpm --filter @aruizca-resume/api run dev\" \"pnpm --filter @aruizca-resume/webapp run dev\"",
    "watch": "pnpm run build && turbo watch build:watch",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "turbo": "^2.5.5"
  }
}
