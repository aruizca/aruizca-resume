{
  "name": "@aruizca-resume/core",
  "version": "1.0.0",
  "type": "module",
  "description": "Core resume and cover letter generation functionality",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "esbuild src/main/resume-generator.ts --bundle --platform=node --format=esm --outfile=dist/resume-generator.js && esbuild src/main/cover-letter-generator.ts --bundle --platform=node --format=esm --outfile=dist/cover-letter-generator.js",
    "dev": "esbuild src/main/resume-generator.ts --bundle --platform=node --format=esm --outfile=dist/resume-generator.js --watch",
    "test": "vitest run",
    "test:unit": "vitest run src/test/unit",
    "test:watch": "vitest",
    "test:watch:unit": "vitest src/test/unit",
    "clean": "rm -rf dist",
    "lint": "echo 'Linting not configured yet'"
  },
  "dependencies": {
    "@jsonresume/schema": "^1.2.1",
    "@langchain/core": "^0.3.66",
    "@langchain/openai": "^0.6.3",
    "ajv": "^8.17.1",
    "jsonresume-theme-even": "^0.23.0",
    "langchain": "^0.3.30",
    "papaparse": "^5.5.3",
    "playwright": "^1.54.2"
  },
  "devDependencies": {
    "@types/node": "^24.2.0",
    "@types/papaparse": "^5.3.16",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0",
    "esbuild": "^0.21.5"
  }
} 